set terminal png
set output outputname


set style data histogram
set boxwidth 0.5
set style fill solid
set xtics rotate by -45


parttime(col) = strftime("%H:%M:%.2S",column(col))
plot  datafile using 1:3:xtic(2) with boxes lt 1 lc 1
