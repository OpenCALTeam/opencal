language: cpp
compiler: 
- g++


before_install:
 - sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y
 - sudo apt-get update -qq


install:
 - sudo apt-get update -qq
 - sudo apt-get install -qq freeglut3-dev libxmu-dev libxi-dev cmake
 
 - wget --no-check-certificate https://cmake.org/files/v3.4/cmake-3.4.3-Linux-i386.tar.gz
 - tar -xzf cmake-3.4.3-Linux-i386.tar.gz
 - sudo cp -fR cmake-3.4.3-Linux-i386/bin/* /usr/local/bin
 
 


before_script:
- cmake --version
- gcc --version
- g++ --version


script: 
- mkdir -p build 
- cd build 
- cmake  -DBUILD_OPENCAL_SERIAL=ON .. 
- make -j4
